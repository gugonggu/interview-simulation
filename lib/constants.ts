export const PASSWORD_REGEX = new RegExp(
  /^(?=.*?[A-Z])(?=.*?[a-z])(?=.*?[0-9])(?=.*?[#?!@$%^&*-]).+$/
);
export const USERNAME_MINLENGTH = 2;
export const USERNAME_MAXLENGTH = 10;
export const PASSWORD_MINLENGTH = 8;
export const PASSWORD_MAXLENGTH = 20;
